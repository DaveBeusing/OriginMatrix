{
  "name": "originmatrix",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "release:check": "node --test && node tools/validate-release.mjs"
  }
}
